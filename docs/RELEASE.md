# 版本与发布策略

## 0.2.10（2026-09-21）修圖互動與切頁優化

整合最新 main 的預排版文字樣式操作、原位分割、完整框剪貼與快捷鍵說明，以及本次修圖優化：背景 Worker 選區運算、正式操作優先排程、局部重繪、魔法棒算法與預覽快取、不同工具游標、畫筆大小滑塊與方括號快捷鍵、魔法棒容差快捷鍵、相鄰原圖預載、跨頁 Worker 重用及圖層記憶體轉移。

本機真圖六次切頁中位數約 256→199 ms，Worker 初始化約 62→9.6 ms；測試使用本機模擬 API 與 StrictMode，不能當作遠端延遲承諾。保存、修訂檢查與快照屏障仍在切頁前完成。診斷 JSON／Worker 計數／A/B 切換僅在開發測試頁顯示，正式 dist 與更新包不包含測試頁。

整合後驗證：135/135 前端測試、238/238 後端測試、lint、正式 build、git diff --check 通過。

本次發布程式碼 Tag 與 GitHub Release，附 application.zip 和 SHA-256；未部署 AutoDL、未製作鏡像、未執行新的 GPU 推理。固定模型、參數和生產工作流保持不變。以下各輪「未發布／未提交」敘述屬開發時驗證紀錄，相關功能在此版本一併發布。

## 0.2.10 預排版整合內容與開發驗證記錄

預排版項目頁頂部新增預設展開、可收起的完整操作表，依一般操作、文字編輯、畫面與滑鼠分組。展開內容限制約 25vh 並可獨立捲動，在窄畫面改為兩欄或單欄，收起後釋放畫布高度。操作表補齊新增、複製、刪除、選取、頁面／底圖、原位編輯、快速樣式、選框按鈕、滾輪及完整框剪貼；不列入尚未實作的 Enter 進入編輯或 Tab 切換下一框。

新增非編輯狀態的單框剪貼：⌘／Ctrl＋C 保存完整框快照而不覆寫系統剪貼簿，⌘／Ctrl＋V 貼完整框到左側畫布指標中心，⌘／Ctrl＋P 以同一樣式貼上系統純文字並保留換行。多選、缺少快照或無有效指標只顯示提示；原位編輯仍使用原生 C／V，且不攔截 P。

本次驗證：88 項前端測試、238 項後端測試、lint 與正式 build 通過。Chrome 合成項目確認頂部操作表、⌘＋單擊、Esc 保存及刷新恢復。隔離頁透過同一鍵盤事件處理器與實際 TextPage／EditorState，確認跨頁指標中心、樣式與分組繼承、換行、一筆撤銷／重做，以及多選、畫布外、空文字與拒絕剪貼簿權限的提示；系統剪貼簿讀取使用可控模擬，未驗收真實系統權限彈窗。

新增 ⌘＋單擊文字直接進入原位編輯，保留雙擊入口及普通單擊選取／拖曳。 Chrome 實際元件驗收通過：橫排／旋轉豎排進入編輯、跨框保留上一框文字、Esc 保存、⌘ 點框旁按鈕、普通點選與拖曳、雙擊入口；lint、正式構建與 238 項後端測試通過。

選取文字框後新增顏色、描邊及方向按鈕；黑字變白字、其他變黑字，描邊在 0／4 間切換，黑字搭白描邊、其餘搭黑描邊。原位編輯可選取部分文字後分割：剩餘文字保留原中心，選中文字繼承樣式另建於右側，整次操作可一次撤銷。未部署遠端、未建立新 Tag。

驗證：80 項前端測試、lint、正式 build 通過；238 項後端測試通過（其中 2 項程序恢復測試在允許程序檢查後重跑通過）。Chrome 隔離頁使用實際 TextPage／InlineTextEditor 驗證三種切換、草稿分割、旋轉直排跨行分割、中心與間距、全選禁用及撤銷／重做；既有 3,675 項 DOM／游標回歸檢查通過。使用合成文字與模擬保存控制器，未修改既有使用者項目。

預排版左右字級標示只顯示數字，不加「目前」或「計算」前綴；缺少數值時仍顯示「—」。

## 0.2.9（2026-09-20）本機啟動與預排版編輯

後續修正直排游標：←／→ 明確換欄、↑／↓ 按完整 Unicode 字元移動，Shift 延伸選字。移除 Selection.modify(line) 在最外欄跳到整段開頭／末尾的行為；不同長度欄位往返保留原字位，空欄及換行逐一移動。保存與游標共用 DOM／文字映射，不改寫輸入中的節點或組字範圍。以使用者提供的三欄文字重現舊版邊界跳字；新版通過各種換行 DOM、不同縮放的瀏覽器回歸，以及原位編輯器新增連續換行後的保存／重開測試。

Safari／WebKit 已知限制：多欄直排的實際插入位置可能正確，但原生游標繪製向下偏移。2026-09-20 以相同字型、文字和程式在 Safari 重現；Chrome 測試頁與正式第 7 頁、300% 縮放下，「和」字後按 ↑ 正確停在「曾」字後。現階段直排原位編輯建議使用 Chrome；重啟後端不會修正 Safari 的繪製問題。參考 [WebKit 286961](https://bugs.webkit.org/show_bug.cgi?id=286961) 與 [310592](https://bugs.webkit.org/show_bug.cgi?id=310592)。

雙擊文字可直接在原位置編輯，支援橫排、直排及旋轉；Enter 換行、點外面或 ⌘／Ctrl＋Enter 完成，Esc 保存並結束編輯。保持字型與幾何樣式，完成的輸入記一次撤銷；保存、匯出與路由切換先提交正在編輯的文字。純文字貼上及中文組字事件與頁面快捷鍵隔離。此版本亦包含本機 `make start-local` 入口與 OCR 字級修正；僅發布程式碼與網頁更新包，未部署 AutoDL 或建立新鏡像。

僅修改「OCR 對齊逐字計算」：CTD 階段同時計算並保留既有單字框字級（相同基準／步長），OCR 校準後取 max（單字框字級，OCR 字級）。measure 保留 font_size_char_box、font_size_ocr 及各自方法，最終方法為 max_char_box_ocr_aligned；缺少可靠 OCR 時保留單字框結果，無可靠單字框時沿用原回退／OCR 結果。OCR 跨行同字的字框 IoU ≥ 0.8 時保留較完整的可靠字框，重複樣本標記 duplicate_overlapping_character 後再做原有 MAD 篩選。獨立「單字框計算」算法保持不變。 使用已保存的 Mask／行框與 OCR 樣本重放問題案例，單字框 26、OCR 28，最終 28；此為計算回歸，非新增 GPU 推理驗收。

左側排版文字右下角常駐目前字級，右側偵測框右下角常駐計算字級；原位編輯時繼續顯示，未知值用「—」。空文字保留可點選範圍，清空後仍能雙擊重新輸入。

本機驗證：lint、build、75 項前端測試、238 項後端測試通過。隔離 Chrome 瀏覽器驗證橫排／直排／旋轉幾何、換行與連續空行、清空後重編、撤銷／重做、Esc、組字事件防護、保存／跨工作區／重開及左右字級標示。量測資料為合成測試值，組字使用模擬事件；未宣稱完成實體中文輸入法或 GPU 推理驗收。重現腳本為 `frontend/tests/prelayout-inline-browser.cjs`，報告與截圖在忽略目錄 `var-test/inline-text/`。

本项目把“代码版本”和“AutoDL 镜像版本”分开管理。Git Tag 用于标记经过测试、可以追溯的代码版本；AutoDL 镜像用于交付已经包含 ComfyUI、custom nodes、模型与应用的完整运行环境。两者有关联，但不要求每个 Git Tag 都制作新镜像。

## 0.2.0（2026-09-12）

整合項目工作台、三種共用合成結果的比較模式、區域導航與懸浮預覽、常駐結果導出入口，以及最新預排版文字控制與頁碼導航。導出前保存並檢查確認狀態，未確認時引導查看，不跳過確認契約。

此 Tag 標記已通過本地 lint、build、195 項後端測試的程式碼；62 項前端測試已在比較與預排版整合時通過。依使用者要求同步既有 AutoDL 無卡實例，保留外部模型、執行環境及使用者資料。此次同步不代表新的 GPU 推理驗收，也不建立 AutoDL 鏡像。以下早期開發狀態為歷史記錄。

## 2026-09-12 项目工作台状态

项目首页、第一部分 Canvas 编辑、项目快照接既有第二部分、第三部分候选比较与局部合成，以及服务端保存／封存导入导出／确认删除已完成本机实现整合。RF-DETR＋MangaLens 的独立 CUDA 执行器和共用 GPU gate 已实现；模型权重尚待上传，目标 CUDA 环境、推理与显存切换尚未验收。

**本次扩展尚未部署、尚未建立新 Git Tag 或 AutoDL 镜像。** 下文 `0.1.0` 仅是既有批次代码基线，不表示项目工作台已属于该版本或已对外发布。本地测试结果也不替代 GPU 验收。

需求和里程碑见 [PROJECT_WORKBENCH_PLAN.md](PROJECT_WORKBENCH_PLAN.md)，实际数据／API 边界见 [ARCHITECTURE.md](ARCHITECTURE.md)，检测模型来源、候选依赖与已知 metadata 例外见 [DETECTION_MODELS.md](DETECTION_MODELS.md)。模型与部署验证未完成前，不将“第一部分 CUDA 可用”写入发行承诺。

## 2026-09-12 預排版擴充

頁碼導覽位於頂部工具列下方，橫向排列並自動換行，不佔左側欄位；頁數較多時導覽區最高佔視窗 25%，其餘頁碼可在區內捲動。既有點頁跳轉、目前頁高亮、連續捲動與顯示切換邏輯保留。

依使用者要求隱藏「上傳去字圖」入口，保留底層匯入功能，繼續使用生成的 inpainted 預覽。

對照模式调整為左側預排版、右側原圖；偵測框增加唯讀單字框及 `W12H12FS22.0` hover 提示。重用既有 CTD／OCR 輸出及来源字級篩選，建立逐頁精簡快取；不重跑模型或改寫 measure。

補齊舊程式的文字快捷鍵：原圖像素移動 1／10／50、字級增減 2／10、旋轉 1°／5°、Option／Alt 滾輪調字級、PageUp／PageDown 與 Q 底圖切換。修正多選旋轉保留各自角度，支援 Mac Option 符號鍵及長按一次撤銷，頂部新增快捷鍵說明。選框左上 −／右上 + 調整字級 2（Option／Alt 點擊 10），左下 ↶／右下 ↷ 旋轉 1°（Option／Alt 點擊 5°），保留上方拖曳旋轉點。普通滾輪繼續上下捲動。8 組離線快捷鍵／撤銷測試通過，瀏覽器實際按鍵及效能仍待驗收；不沿用先前拖曳數據作為此次快捷鍵實測。

依後續需求，預排版僅匯出 `Meo.json`，移除「導出項目」按鈕及項目 ZIP 匯出 API。伺服器保存及既有封存匯入保留，使用者關閉網頁後可重開原項目。

後續補接 CTD 核心的 `inpainted` 生成：用來源 OpenCV Telea 参数產生透明覆蓋層並疊回原圖，整批驗證後自動用作預排版底圖。無須上傳去字圖或新增模型，原圖與文字保持不變；僅屬本地預覽功能，未新增修圖工作流或資料共用。

本地測試新增明確選用 Apple GPU（`COMIC_PRELAYOUT_DEVICE=mps`）的路徑，可引用已有外置模型及 Python；預設 CUDA、GPU 任務互斥與不回退 CPU 的約束保持。真實本地模型結果另記於下方驗證文件，不作 CUDA 鏡像發布依據。

工作樹 `codex/prelayout-web` 在既有工程內增加獨立資料的網頁預排版，包含連續頁面、分級預覽、BT／LabelPlus、文字與多選編輯、草稿／版本衝突、唯讀量測匹配、封存重開，以及外部 CTD／OCR 背景任務。沒有引入角度測量工具、measure 編輯、獨立倉庫或桌面打包。

本機證據集中在 [PRELAYOUT_LOCAL_VALIDATION.md](PRELAYOUT_LOCAL_VALIDATION.md)。程式以 `codex/prelayout-web` 本地分支記錄，本次未部署、推送或建立 Tag；既有 `0.1.0` 與先前修圖 GPU 成功記錄不能代表此擴充已發布。CTD／OCR 的真實 CUDA、小批次、顯存切換與乾淨鏡像驗收仍是發布門檻，詳見 [部署準備](DEPLOYMENT.md#預排版部署準備)。

發布還須確認：預排版和修圖封存不跨資料引用；固定字型與指標匹配；模型及推理環境在工程外；公開鏡像清理涵蓋預排版圖片、文字、匯入稿、常用框、任務及暫存 ZIP；保留所遷入核心及外部資產的來源授權材料。

## 基本原则

- 本地是主要开发环境；完成修改和本地测试后才推送 GitHub。
- AutoDL 实例是集成与 GPU 验证环境，不直接作为唯一源码来源。
- 远端必须部署某个明确的 Git commit，不使用无法追溯的临时文件组合。
- 远端测试通过后，给这个确定的 commit 打 Git Tag。
- 只有形成有意义的用户交付内容，或运行环境发生变化时，才制作新的 AutoDL 镜像。
- Git Tag 本身不会更新已经创建的 AutoDL 实例。没有独立应用升级机制时，正式镜像可以合理地落后于 GitHub 代码版本。

## 标准开发流程

```text
本地开发
  ↓
本地 lint／build／后端测试／固定样例测试
  ↓
提交并推送 GitHub main
  ↓
把明确的 commit 部署到 AutoDL 测试实例
  ↓
按改动风险做无卡或 GPU 集成测试
  ↓
测试不通过：回本地修改并重复以上流程
  ↓
测试通过：打 Git Tag 并推送
  ↓
判断是否达到 AutoDL 镜像发布条件
```

不要在远端测试实例直接完成长期保留的代码修改。紧急排障若必须临时修改，应尽快把同一修改带回本地仓库、重新测试并提交，避免 GitHub 与镜像内容分叉。

## Git Tag 规则

代码 Tag 使用不带 `v` 的语义版本：

```text
0.1.0
0.1.1
0.2.0
1.0.0
```

- Patch，例如 `0.1.1`：问题修复、兼容调整、小型界面优化。
- Minor，例如 `0.2.0`：新增 Mask 生成、结果挑选、任务管理等向后兼容功能。
- Major，例如 `1.0.0`：正式稳定版或存在明显不兼容变更。

Tag 应使用 annotated tag，说明主要内容和验证范围，并指向已经通过远端测试的准确 commit。不要为每个普通提交打 Tag；Tag 表示一个可识别、可回退、可供镜像选择的稳定节点。

打 Tag 前确认：

1. 工作树干净，目标 commit 已推送至 `main`。
2. `config/runtime.json` 的 `app_version` 与准备发布的 Tag 一致。
3. 前端 lint 和 build 通过，后端测试通过。
4. 需要远端验证的改动已完成对应测试。
5. 仓库不含 Token、私钥、用户图片、生成结果、日志或模型权重。

标准命令：

```bash
git tag -a 0.2.0 -m "0.2.0：版本摘要"
git push origin main
git push origin 0.2.0
```

不要移动或覆盖已经公开的 Tag。发布后发现问题时，修复并创建新的 Patch Tag。

## 是否需要远端 GPU 测试

| 改动类型 | 本地验证 | 远端验证 |
|---|---|---|
| 文案、颜色、静态排版 | lint + build | 6008 页面检查即可，可无卡 |
| 普通前端交互、历史列表、下载提示 | lint + build + API mock/测试 | 6008 集成检查，通常不需要 GPU |
| FastAPI 接口、任务恢复、打包逻辑 | 后端测试 + 集成测试 | 远端小批次，确认持久化和下载 |
| 项目修订／不可变快照、完整封存／删除 | 版本冲突、移目录重开、路径／哈希／引用检查、活动任务与下载互斥 | 6008 集成验证，确认旧 jobs 兼容；快照输入边界另做受影响 GPU 回归 |
| Canvas 编辑与局部合成 | 原尺寸坐标、撤销／保存、来源分配、确认与导出一致性 | 真浏览器操作及服务器保存后羽化预览；不以模拟候选替代模型验收 |
| RF／MangaLens 与共享 GPU gate | CPU 分类、edited 保护、缺模型隔离、取消／重启进程互斥 | 两模型真实 CUDA、小批次与 ComfyUI 双向切换显存验证 |
| Mask 生成、二值化、方向或配对 | 固定样例与边界测试 | 必须跑受影响工作流的 GPU 小批次 |
| 生成图片发现、命名、挑选或归档 | 固定目录样例 + 打包测试 | 必须跑受影响工作流并核对结果数量 |
| 批次器、RGBA、Crop/Stitch | 单元与集成测试 | 必须做真实 GPU 回归 |
| 工作流、模型、custom node、ComfyUI、PyTorch/CUDA | 能做的静态校验 | 必须做三工作流 GPU 回归 |

Mask 与输出挑选虽然不一定修改工作流 JSON，但位于推理输入和结果完整性边界，按高风险应用逻辑处理。Mask 测试至少覆盖尺寸一致、黑白方向、全黑、全白、透明输入、多图 stem 配对和重新选择覆盖；输出挑选至少覆盖多候选结果、stem 对应、缺图、重复输出、用户选择与最终 ZIP/PDF。

## 项目工作台的新增验收项

- 首页可新建、重开与重命名项目；每项目包含原图；已有 Mask 能跳过第一部分直接提交，不要求安装检测模型。
- 页面保存保留完整颜色、other 和 edited；过期请求不覆盖新修订，重开后与保存一致。提交使用不可变 snapshot，后续编辑不改变已提交结果依据。
- 全黑整批不等待 ComfyUI、不载入修复模型，沿用当次底图且所有选中工作流齐页；不生成空比较 PDF。
- 检测 Python 与 Web／ComfyUI 环境分离；缺权重仅停用检测。逐阶段进程退出后才切换模型；取消和服务重启不能提前释放仍在使用的 GPU。
- 第三部分只接完成 run，以其快照比较候选；保存像素来源分配、羽化和逐页确认。画布即时来源预览与保存后的羽化预览要分别验证，成品必须匹配服务器预览。
- 完整项目封存可移到另一数据根目录导入并续编，不依赖原服务器绝对路径，不恢复 GPU 任务。只导出成品、完整项目包及第二部分候选包是不同输出。
- 删除只能作用于明确归属项目的资产和 jobs；活动任务／下载时拒绝，保留旧版独立 jobs 及模型。首页“旧版批次与历史”仍能读取和下载原任务。

运行时依赖有变化时按本文件流程安排新 GPU smoke test 和受影响小批次；第二部分已被用户确认可用，不把重复原基线验收作为本地开发前置，也不能跳过本次新增的快照、Mask 与 GPU 切换边界。

## AutoDL 镜像发布条件

不是每个代码 Tag 都需要新镜像。以下改动可以积累到下一个有意义的镜像版本：

- 文案、颜色和小型布局调整；
- 不影响推理与数据结构的普通网页功能；
- 文档与开发规范；
- 仅供后续开发使用、尚未准备交付普通用户的功能。

以下情况应当制作新镜像：

- Mask 生成或结果挑选已经形成准备交付用户的完整功能；
- 修复会影响普通用户正确性、启动或任务安全的重要问题；
- 改动输入转换、Mask 方向、Qwen RGBA、批次器或结果结构；
- 新增或改变 Python、前端、系统依赖；
- 修改 ComfyUI、custom nodes、模型、LoRA 或工作流参数；
- 积累了一批用户可感知功能，准备形成新的发布里程碑。

因此，是否发布镜像既看技术依赖，也看是否准备把这批功能交付给普通用户。小代码也可能是必须发镜像的关键修复；大量内部文档改动则不需要镜像。

## 镜像发布流程

1. 选择一个已通过远端测试的 Git Tag，记录 Tag 和完整 commit hash。
2. 确认测试实例运行的应用文件确实来自该 commit。
3. 运行与改动匹配的 GPU 回归；运行环境变化时执行完整三工作流回归。
4. 验证 6006、6008、自动启动、任务恢复、结果 ZIP 和 PDF。
5. 确认没有活动修复、检测、封存或下载，重要项目及测试结果已取回；`active_job_id` 与 `gpu_owner` 均为空。
6. 先执行 `./deploy/prepublish-clean.sh` 只读预览并检查每个目标；清单必须覆盖 `COMIC_DATA_ROOT/projects` 和 `jobs`，不能遗漏原图、编辑修订、快照、检测缓存及项目封存。
7. 明确确认后执行 `./deploy/prepublish-clean.sh --apply`。
8. 执行 `deploy/verify.py`，核对 ComfyUI、节点、工作流、模型与前端构建。
9. 再次确认镜像中没有凭据、用户项目及封存、生成结果、运行日志和 Shell 历史；不可直接复制包含数据目录的本地工作树作为公开镜像内容。
10. 在 AutoDL 控制台保存新镜像，记录镜像版本与 Git Tag 的对应关系。

建议每次镜像发布记录：

```text
AutoDL 镜像版本：
Git Tag：
Git commit：
发布日期：
GPU 与显存：
ComfyUI／Python／PyTorch／CUDA：
custom node 版本：
模型与工作流校验：
检测 Python／CUDA／依赖例外及权重校验（若启用第一部分）：
项目／快照／封存格式与旧 jobs 兼容：
回归批次与结果：
已知问题：
```

## 应用版本与镜像版本

当前阶段使用 Git Tag 表示应用代码版本，例如 `0.1.0`。AutoDL 镜像在平台上单独编号，并在发布记录中写明它包含的 Git Tag，不强制复用同一版本号。

未来若增加独立应用升级器，可以进一步拆成：

```text
App 0.3.0
Image Runtime 1.1.0
```

应用更新只替换 React、FastAPI 和普通编排代码，并只重启 6008；运行时镜像继续固定 ComfyUI、节点、模型和 GPU 环境。在升级器完成之前，不允许启动脚本自动拉取 GitHub `main` 或 `latest`，以免公开镜像在无人验证的情况下改变行为。

## 既有首版基线（不包含本次未发布扩展）

- 代码版本：`0.1.0`
- 内容：三套生产工作流批量网页、原图与独立 Mask 输入、单 GPU 串行执行、任务恢复、显存与耗时记录、结果 ZIP 和三工作流比较 PDF。
- 目标运行环境：AutoDL 单张 32 GB GPU，6006 提供 ComfyUI，6008 提供用户网页。
- Git Tag `0.1.0` 表示第一版代码基线；是否公开为新的 AutoDL 镜像仍按上述镜像检查流程单独决定。

## 文件夾讀取修正（未發布）

工作台與舊版批次改為非遞歸目錄讀取，子目錄不會被開啟或枚舉；目錄按鈕授權失敗時可拖入文件夾，亦保留多選圖片。新增 `npm --prefix frontend test`，驗證子目錄及排除檔案不被讀取、拖入目錄的多批次讀取與失敗處理；4 項通過。本次只更新本機前端構建，未部署或建立版本。內建瀏覽器曾在使用者選目錄後回傳 AbortError；不能以單元測試通過宣稱該瀏覽器原生目錄對話框已修復。

## 第一部分操作與本機模型（未發布）

恢復左側 Mask／原圖與右側即時填色預覽、待修補顏色／透明度和偵測文字資產。新增顯式 MPS／CPU 執行裝置，CUDA 預設及三套修復工作流不變。本機模型驗證結果另記錄，不代替目標 CUDA 鏡像驗收。

## 工作台匯入與檢測介面簡化（未發布）

自動檢測按鈕與進度隱藏模型、裝置技術名稱；新建項目沿用「去四邊」的單一匯入區與多圖／單資料夾選單。標準資料夾輸入可能由瀏覽器列舉子目錄，前端只匯入第一層；拖入仍不遞歸。新建後移除外部 Mask 匯入，建立時的可選 Mask 保留。僅本機前端更新，未部署 AutoDL。

本機驗證：lint、正式 build、77 項後端與 7 項前端測試通過。內建瀏覽器選取 `17` 資料夾得到 21 張；改選兩張獨立圖片後為 2 張。另以含子目錄的測試資料夾驗證只選到根層 2 張圖片、Mask 模式僅 1 張 PNG。檢查編輯頁保留雙側畫布，沒有外部 Mask 匯入。未啟動模型推理或建立測試項目。

修正 React 19 下靜態 Modal.confirm 點擊無反應：項目刪除改受控視窗並顯示失敗原因；重命名與新舊批次放棄任務改用 Modal.useModal。瀏覽器已驗證開啟與取消；隔離記憶體項目驗證 409 錯誤保留及重試成功移除。lint、build、77 項後端與 7 項前端測試通過。只更新本機靜態構建，沒有刪除使用者項目、部署或提交。

## 第一部分編輯工具恢復（未發布）

提供矩形／畫筆的添加、減去與局部交集；魔法棒四種操作、容差、擴展及實際差量預覽；多邊形套索；可調 ROI 的局部副本。右鍵框選清除兩類 Mask，Cmd／Ctrl＋右鍵轉入另一類，並保留可見入口。移除「擦除並保留原圖」類別。修正工具切換時操作不相容、同 Mask 重填色、取消局部後待保存計時器及局部刷新保護。

已用隔離服務的合成圖片測試矩形保存、右鍵清除、Cmd 右鍵轉入、魔法棒孔洞預覽／套用、套索閉合、畫筆局部交集、局部框邊拖動、取消、套用與一次撤銷；核對保存圖層像素。未操作使用者現有項目，未執行模型，未部署／提交。詳見 [EDITOR_TOOLS.md](EDITOR_TOOLS.md)。

整理編輯工具列：F1／F2 常駐、記住選擇、編輯／工具／操作分排、保存與縮放分組。轉入保留原工程「另一類 → 目前類別」語義，框選開始固定目的類別並顯示方向。隔離瀏覽器實測轉入 F2 時純色 -1／待修補 +1，切 F1 後恢復原數量；刷新仍維持 F1。未更動使用者項目。

2026-09-12 後續修正：依使用者確認，Cmd／Ctrl＋右鍵改為框內兩類 Mask 同時互換：純色填充轉待修補、待修補轉純色填充，空白及框外不變，與目前 F1／F2 類別無關。僅保留 Cmd／Ctrl＋右鍵拖框手勢，移除轉入按鈕及 F12 綁定，局部視窗受 ROI 限制。保留即時雙側預覽、手勢一次撤銷及自動保存。此規則取代上述單向轉入語義，也不採用有／無 Mask 取反。


2026-09-12 同步原工程 `be48a98` 的氣泡內外分類、白色描邊外圍檢查與氣泡擴展限制。Cmd／Ctrl＋右鍵仍為雙向互換，但純色→待修補僅轉文字修補與人工添加範圍，撤掉多餘氣泡填色。加入「自動檢測」頁面內覆蓋確認，確認後重建整個項目的第一部分圖層，原圖及既有修復結果保留。API 的 `replace_existing` 預設 false，前端確認後傳 true。新輸出驗證完成後才開始回寫。

驗證：30 項前端測試、122 項後端測試、lint、正式構建通過。真實單頁與固定上游版本的 overlay／other／背景取樣／diagnostics 完全一致（7 純色、1 待修補、5 氣泡擴展）。隔離瀏覽器確認文字可轉待修補、氣泡背景只移除填色、撤銷恢復；确认取消不提交、確認覆蓋參數為 true、失敗提示保留。未改寫使用者圖片，未重跑模型或部署 AutoDL。

2026-09-12：新建項目與自動檢測確認視窗加入共用設定（Mask 膨脹、塗白範圍、氣泡辨識、氣泡內縮）。設定隨項目保存、重新開啟及封存匯入；新建不執行模型。氣泡辨識關閉時完整跳過 MangaLens。前端 lint／build、30 項前端測試及 152 項後端測試通過。隔離瀏覽器實測新建保存、重新開啟和確認提交參數；未執行新的模型推理或 AutoDL GPU 驗收。
## 邊緣塗白本機增量

新增獨立集合／頁面、自動線位搜尋、逐頁保存及 PNG／桌面線位 JSON 輸出。未建立版本 Tag、未推送、未部署、未發布鏡像。完整集合 ZIP 匯入及與修復項目自動交接不在本次版本中。

檢查增加 `npm --prefix frontend test`，涵蓋 Swift 基線搜尋、原尺寸網格、Worker 來源快取及非遞歸目錄讀取。另執行 lint、正式 build、全部後端 pytest；瀏覽器驗證匯入、拖線、吸附、撤銷、保存／續編及下載。驗證紀錄見 `docs/EDGEWHITE_LOCAL_VALIDATION.md`。

發布候選必須保留 `/edgewhite/` 的使用者資料備份與隔離，包含 Worker 產物，確認新集合第一層目錄枚舉不會讀取任何子資料夾。`make verify-local` 的 ComfyUI／模型檢查仍屬整體鏡像驗收，缺少模型的本機不能宣稱通過鏡像驗收。


### 2026-09-12 匯入入口修訂（取代此前非遞歸選取器方案）

使用者已允許瀏覽器掃描子目錄。新建集合改為單一匯入區：點擊唯一匯入區後，在選單選擇「多張圖片」或「單個資料夾」，也可直接拖入；資料夾模式使用標準 `input webkitdirectory`，不再呼叫 `showDirectoryPicker`。瀏覽器可列舉子目錄，應用只匯入第一層圖片，避免包含 deal 成品。因瀏覽器原生檔案選擇器區分圖片多選及目錄模式，兩種模式由同一入口的選單選取，不再有獨立資料夾按鈕。

內建瀏覽器實測：經資料夾選擇器指定第 82 話目錄，顯示 21 張並略過 1 個子資料夾；點擊主區開啟多選選擇器，選入 2 張測試圖片成功。此前 showDirectoryPicker 的阻塞不再適用於新版入口。lint、build、18 項前端及 61 項後端測試通過。

## 三個工作區合併（2026-09-12）

`codex/prelayout-web`（8cdc3c7）及 `codex/edgewhite-web`（74b4ec8）合入 `codex/project-workbench`。修圖仍為預設入口，首頁增加預排版和邊緣塗白；三部分沒有強制前後依賴。統一的 hash 路由在離開編輯頁前保存，失敗則留在原頁。修圖頁保持掛載並在隱藏時禁止互動，返回時保留原圖片與步驟；附加模組按需載入。

資料分別位於 `<COMIC_DATA_ROOT>/projects`、`prelayout`、`edgewhite`，API 使用獨立路徑；合併不搬移其他工作樹的試用資料。預排版自訂根目錄不得重疊修圖、jobs 或 edgewhite。CTD／OCR 與修圖偵測／ComfyUI 共用 ResourceGate，程序恢復時保留全部仍存活的佔用。原修圖工作流、批次器、輸入轉換與合成核心未改。

整合驗收：前端 lint／build 與 58 項測試通過；後端 194 項測試通過。隔離瀏覽器使用合成圖片驗證三個入口與編輯頁、預排版文字修改跨模組返回後仍保存、返回修圖仍停在批量修復步驟。三個 API 清單各只包含自己的測試項目；無 GPU 任務。`make verify-local` 的倉庫工作流與前端通過，本機 /root/ComfyUI 缺 19 項外部環境；未做新的 CUDA 驗收。

## 手塗工作流副本

新增「Flux手塗去字」、「FireRed手塗去字」、「Qwen手塗去字」，使用 ComfyUI 原生遮罩編輯器。原三套生產工作流及網頁批次配置保持不變；使用與部署方式見[手塗工作流說明](../workflows/handpaint/README.md)。目前完成結構檢查，GPU 推理驗收待有卡環境執行。


<!-- comparison-view-review -->
比較合成交互更新：第一張為唯一成品預覽，成功顯示即確認；切頁保留視圖設定，候選面板直接選取採用。所有頁面已確認才可輸出，匯出不代替瀏覽確認。詳細行為見 `docs/COMPARISON_PARITY.md`。本輪僅本地構建與測試，尚未部署遠端。


比較合成提供「多圖對比」「整體＋局部」「區域卡片」，同一瀏覽器記住上次模式。三種方式共用 uint16 像素來源分配、保存、羽化及輸出；切換前保存，失敗時保留原模式。新區域模式使用現有候選及差異 Mask，無新增模型或 GPU 流程；只更新前端構建即可。詳見 `docs/COMPARISON_PARITY.md`。

項目頁右上常駐「導出結果」，與「導出項目」並排。導出先保存並讀取最新合成狀態；仍有待確認頁面時顯示數量並前往第一張待確認頁，不自動替使用者確認整批。全部確認後下載結果 ZIP。僅前端更新。


### 2026-09-13 新建與匯入觸發時機

新建預排版上傳完成後立即詢問 CTD 識別；LP.txt 確認匯入時，若已有完成的 CTD 結果，自動匹配本次匯入頁面並一併保存，其他頁面保持原狀。塗白新建項目可選「建立後立即自動檢測」（預設勾選），上傳建立成功即提交；勾選時明示會取代匯入 Mask。啟動失敗保留已建立項目並顯示可重試錯誤。


### 0.2.1 應用更新檢查

新增所有工作區共用「檢查更新」，目前應用版本 0.2.1；按鈕只查詢固定 GitHub 倉庫的正式數字 Tag，按語義版本比較，排除預覽版，不自動安裝。結果快取 60 秒，網路失敗明示無法檢查。此次應用升級包含新建時啟動檢測及 LP 匯入自動匹配；版本以 0.2.1 Tag 對應此次應用發布。部署更新 backend/app 及前端 dist，保留模型、環境、使用者資料；確認無活動任務後只重啟 Web。

無卡開機的 nvidia-smi 可能拋出 OSError（Exec format error）；健康檢查將其視為 GPU 不可用，仍回傳正常應用狀態。驗證：lint、build、62 項前端測試、199 項後端測試通過；AutoDL 無卡 6008 重啟及健康檢查正常，未執行新 GPU 推理。


### 0.2.2 網頁升級

0.2.2 加入 6008 網頁安裝器：保存編輯後確認升級，只接受固定 GitHub 倉庫的較新正式 Tag。獨立程序下載 application.zip 與 SHA-256，逐檔校驗並檢查執行環境需求；只更新 backend/app、backend/imaging、backend/prelayout_core、frontend/dist，不覆蓋模型、環境、設定或資料。更新時封鎖其他應用 API，已有請求／GPU 任務時拒絕；備份後只重啟 6008，新版啟動失敗自動回復。狀態與備份存於資料根 updates/。發布前先 build，再用 deploy/build-update.py 產生更新附件並上傳該 Tag 的 Release。0.2.1 需先部署此安裝器一次，後續由網頁更新。


### 0.2.3 批量修復計時

批量修復顯示每秒更新的「已運行」，從任務建立起包含準備、模型載入、生成與打包；完成、失敗、放棄時固定「總耗時」。後端保存 finished_at，之後修改其他 metadata 不會延長耗時，舊紀錄使用 updated_at 相容。計時由模擬任務測試驗證，不需要實際執行 ComfyUI；此版作為 0.2.2 → 0.2.3 網頁升級驗收目標。


### 輸出完整性修復（2026-09-17）

ComfyUI 輸出先複製到暫存檔，通過 PNG 完整性與解碼檢查後才發布；流程結束重新同步完整原檔，修復過早複製的副本。比較 PDF 為附加輸出，失敗仍打包圖片與日誌並完成任務，完成訊息保留警告。此修復只需部署後端並重啟 6008，不重啟 ComfyUI。

0.2.4：項目工作台「建立新的修復版本」預設勾選 Flux2 Klein 與 FireRed，Qwen 可自行勾選；舊版批次預設不變。此版本一併包含 PNG 完整性與 PDF 非阻斷修復。

0.2.5：6008 網頁增加 ComfyUI 狀態及手動重啟按鈕。操作保留日誌並核對受管理的 6006 PID，與生成、偵測及升級共用 GPU 互斥；無 GPU 或有任務時拒絕重啟，不自動重跑失敗任務。反覆退出原因仍待定位，此功能是恢復入口。

服務連續失聯時終止批次等待、保存服務日誌並最多自動重啟一次；同一任務恢復次數持久保存，再次失敗由使用者決定。6008 可下載部分結果、手動續跑或明確接受已有候選進入合成；缺少候選會標示，沒有任何候選的待修補頁仍阻止完整導出。手動續跑沿用原任務不可變輸入，只補算缺少／檔案損壞的輸出。


### 0.2.7 操作指南與小型工作台調整

操作指南補齊漫畫去字修復、LabelPlus 預排版與邊緣塗白的分步截圖；純色填充改從原圖背景取樣並移除工具列選色器；預排版預設開啟原圖對照、去字底圖與偵測框。此 Tag 不製作新 AutoDL 鏡像，也不代表新的 GPU 推理驗收。

0.2.7 發布後發現 AutoDL／SeetaCloud 代理可能把公開 `Origin` 與內部 `Host` 轉成不同值，導致合法更新誤回 403。後續修正以瀏覽器 `Sec-Fetch-Site: same-origin` 驗證同頁請求，並以 `X-Forwarded-Host` 相容舊瀏覽器；相同修正套用至 ComfyUI 重啟入口，跨站請求仍拒絕。既有 0.2.7 無法透過被阻擋的按鈕自我取得此修正，需先人工部署後端或發布包含修正的新版本。

### 0.2.8 反向代理更新修復

0.2.8 收錄上述同頁判斷修復。遠端 0.2.6 已先人工熱修後端，公開網址的合法更新驗證由 403 變為版本檢查 400，跨站請求仍為 403；只重啟 6008，6006 PID 保持不變。另修正 0.2.7 Tag 內 `APP_VERSION` 仍為 0.2.6 的發布疏漏：0.2.8 同步後端與 runtime 版本，更新包建置器強制檢查版本一致、HEAD 具同版本 Tag、工作樹乾淨。發布時必須建立 GitHub Release 並附上 `application.zip` 與 SHA-256；只有 Tag 不能升級。

### 0.2.6 新建項目保留上傳 Mask

漫畫修圖項目接受部分頁面的同檔名 Mask；拒絕多餘 Mask、重複檔名與尺寸不一致。新建時只自動檢測缺少 Mask 的頁面，已提供的 Mask（包含全黑）保持不變；全部已有 Mask 時不啟動檢測。檢測未啟動或未完成可按「補充缺少的 Mask」重試；原有明確確認的重新檢測仍可取代圖層。此版只本地測試並發布 GitHub 更新包，未部署遠端、未執行 GPU 推理，無新增模型或依賴。

本地驗證：67 項前端測試通過；後端 233 項通過（包含新增 Mask 保留測試），既有 `test_live_recovery_process_group_keeps_gpu_until_children_exit` 在此機重跑仍發生等待逾時。該測試及預排版進程管理程式本次未改動；0.2.5 獨立目錄對照測試通過，逾時差異尚未定位，不將整套後端測試標示為全數通過。

## 未發布：修圖選區互動隔離

`codex/selection-responsive` 將第一部分與局部視窗的輸入輪廓、Mask 計算和預覽拆開：左側立即畫幾何輪廓，正式編輯和 PNG 快照由 Worker 依序完成，左右像素預覽可延後。保留原有 Mask 運算語意與保存 API；第三部分候選合成、模型流程、提示詞和參數不變。

合入前核對 [選區互動驗證](SELECTION_RESPONSIVENESS.md) 的背景延遲測試、像素一致性、撤銷／保存／重開與局部視窗流程。尚未推送、部署或建立 Tag；既有鏡像沒有自動取得這項改造。

修正魔法棒懸停世代誤使正式畫面失效，加入可記憶的預覽開關，點擊重用相同預覽運算；本機測試頁預設取消人工回覆延遲。畫筆增加大小滑塊及可見 `[`／`]` 提示，快捷鍵也支援第一部分與局部視窗。

魔法棒啟用時，`[`／`]` 改為減少／增加容差，每次 1，限制在 0–100；工具列顯示對應提示，容差改變後重新計算懸停預覽。畫筆仍每次調整 4 px；輸入框與 Ctrl／Cmd／Alt 組合不攔截。

未發布的第二輪選區優化包含可丟棄預覽排程、正式操作優先、基於 revision 的局部重繪，以及保持像素一致的魔法棒／橢圓 morphology 加速。詳見選區驗證報告；當前仍在獨立工作樹，未部署。

未發布追加：矩形／畫筆／魔法棒獨立游標；第一部分當頁及後兩頁、前一頁原圖預載，64 MiB 有界解碼快取。原圖快取不涵蓋可編輯圖層，不改保存及修訂衝突流程。

未發布追加：完整切頁分段量測、項目內主編輯 Worker 重用，以及 owned 圖層 transfer／Worker adopt。保存與 revision reload 仍在切頁前完成，局部編輯維持獨立。首開仍需建立 Worker；收益主要在同項目的後續切頁。

## Qwen 2.1 與雙 Release 整合（待 GPU 驗收）

此次使用者授權將 Qwen 2511 批量與手塗兩入口替換為 Qwen Image 2.1 INT8。Flux、FireRed 模型與工作流參數保留。相容鍵／結果目錄 `qwen2511_lanpaint` 保留，介面顯示新模型；舊工作流另存本地備份，不作新工作流載入。舊章節中的 Qwen 2511 效能與 RGBA 規則僅適用歷史版本。

整合環境採已有的 ComfyUI 0.37.0 原生 Qwen2.1 程式與 PyTorch 2.14.0+cu130 隔離環境，仍只開正式 6006 服務。`COMFY_PYTHON` 指向 `/root/comfy-qwen21-venv/bin/python`；啟動指定 BF16 text encoder／VAE 計算，DiT 及文字編碼器權重仍為 INT8。公共庫模型優先復用。不能把修改 JSON 當成舊 0.34 鏡像已具備新模型支援。

雙平台發布及更新規則見 [DUAL_RELEASE.md](DUAL_RELEASE.md)：同一份更新包與 SHA-256 同步 GitHub／Gitee，環境不符時拒絕套用。此次以 0.2.11 同包發布，GPU 回歸由使用者發布後開機測試。無卡檢查不能替代 GPU 驗收；有卡後先一組非空 Mask，再驗證三模型串行切換、黑 Mask 直通、時間／顯存記錄及下載。

## ComfyUI 圖片清理

頁面上方的「清理 ComfyUI 圖片」可查看 input／output／temp 的圖片數量、容量與預覽。已完成且成品完整保存到網頁項目的副本可整批清理；手動／未知圖片按日期篩選及選取後另行確認。清理採使用者掃描時的檔案清單與簽章，刪除前重新核對，不會順帶刪除掃描後新產生的圖片。未完成任務副本、模型、工作流、網頁項目內成品均不屬獨立清理範圍。

刪除網頁項目時先清除該項目確切 job ID 所屬的 ComfyUI 副本；清理失敗保留項目供重試。舊版沒有任務前綴的 Qwen RGBA 不自動推斷歸屬，留在未知圖片清單。應用 GPU gate、修復佇列及 ComfyUI 原生佇列任一忙碌即拒絕清理；直接操作原生 ComfyUI 的維護者也應避免在清理過程提交新工作。
