# 修圖選區互動驗證（2026-09-20／21）

## 範圍與原則

分支 `codex/selection-responsive`，從 `96d3b02` 建立獨立工作樹。修改修圖第一部分及局部副本的 edit 模式；不修改其他工作樹、預排版、第三部分候選合成或 GPU 工作流。

左側輸入優先：拖曳期間只更新幾何輪廓，正式像素處理與右側重繪可延後；正式操作不能丟失或錯序。這一版採用 SVG 輪廓 + Canvas/ImageBitmap + module Worker，尚未採用 WASM。Worker 內仍保留原有全圖運算與有上限的歷史快照；更小的記憶體占用、局部區塊計算及 WASM 需另按實测瓶頸決定。

## 實作

- 高頻移動只記錄原圖座標；畫筆保留可用的 coalesced events。矩形、畫筆、套索完成時才提交正式指令，取消不進歷史。
- Worker 持有正式圖層、採樣來源與歷史，依序處理編輯、撤銷、重做、局部合併及保存快照。PNG 編碼也在 Worker，快照完成前後續指令不得超越。
- 左側獨立 SVG 顯示正在操作和等待中的輪廓。預览最多一個在途请求，後續需求合併；過期 ImageBitmap 釋放。操作中不替換完整像素畫面，待手勢／套索結束才呈現最新有效畫面。
- 保存以 UI 提交版本為屏障，等待已接受的指令；保存期間又有提交則繼續保存。未完成手勢仍阻止離頁，但不妨礙已保存版本清除舊的保存中狀態。Worker 致命錯誤不回報保存成功。
- 主執行緒預先計算佇列末端的撤銷／重做數量，避免快速操作或空撤銷產生錯誤狀態。

## 獨立驗證分工

使用 3 個 Sol high 子代理：A 檢查左側互動並建立瀏覽器測試頁；B 實作及檢查 Worker、失敗處理和排程；C 獨立加入像素一致性／排序／生命週期測試並審核整合。主代理整合程式，另外在獨立 IAB 頁面完成互動及保存重開測試。

審查修正包含：Worker 失敗後新請求不能永久等待；空撤銷不製造保存版本；完整預覽畫面不搶佔正在進行的手勢；保存完成與新手勢交錯時正確解除舊保存狀態。

## 自動化驗證

- `npm --prefix frontend test`：88 項通過，包含 13 項新的 Worker 測試。
- `npm --prefix frontend run build`：正式構建通過，產生獨立 raster-worker chunk。
- `npm --prefix frontend run lint`：通過，0 errors、0 warnings。
- 後端 pytest：236 項在沙箱內通過；2 項程序恢復測試需要 `ps`／程序群組權限，解除該限制後單獨重跑均通過，合計 238 項。

新的測試涵蓋矩形、多邊形、畫筆、魔法棒、局部裁切、清除／互換與既有核心逐像素比對；撤銷／重做、局部合併、預覽及快照不可變；非同步 PNG 快照排序；延後／亂序回覆關聯、過期 bitmap 回收、Worker 致命錯誤和卸載。

## 瀏覽器測試方法與證據

啟動前端 Vite 後開啟 `/tests/selection-responsive-browser.html?workerDelay=1000`。預設合成測試圖為 2048 × 3072，`preset=square4k` 可用 4096 × 4096。頁面使用真正的 RasterEditor；Worker 回覆延後 1000 ms，讓正式結果刻意落後左側操作。它不連接使用者項目，也不提交伺服器資料。

頁面提供開始／讀取量測、額外 Worker CPU 壓力、保存及保存並重開。量測指標是 pointer 事件到下一次 SVG DOM 更新，不代表實體螢幕的 photon latency。初始解碼、初始化、首次全圖預覽需要先完成，再開始暖機後量測。

主代理 IAB 驗證：2048 × 3072、1000 ms Worker 回覆延遲、兩筆連續畫筆。19 個 pointer-to-SVG 樣本約 12.9–40.5 ms，量測區間無 >50 ms 主執行緒 long task。該自動化瀏覽器 rAF 約每 41.7 ms（約 24 Hz），因此此結果證明輸入沒有跟著 Worker 等一秒，不能宣稱已驗收 60 FPS。另一次完整流程連續畫兩筆、撤銷第二筆、保存並重開，畫面正確只保留第一筆，保存狀態正常。

子代理 A 的獨立 Chrome 補充測試把 Worker 回覆延後 3000 ms：2048 × 3072 畫筆 10 樣本為 4.7–47.8 ms、矩形 9 樣本為 19.2–45.4 ms、套索 6 樣本為 5.7–49.2 ms；結果回來前介面仍顯示正在處理。4096 × 4096 畫筆 10 樣本為 8.3–63.3 ms，矩形 10 樣本中 9 個為 7.9–46.9 ms，但有一個 144.7 ms 離群值；部分量測也記錄到長任務。瀏覽器擴充控制和測試傳輸會干擾計時，尚未完成長任務歸因，因此只能確認互動不等待 Worker，不能據此宣稱大圖完全不卡。A 的 Chrome 真圖第 7 頁上傳因檔案選擇器停滯未完成；真圖瀏覽器證據由主代理 IAB 測試提供，B 另做真實像素核心比對。

## 指定漫畫真圖驗證

使用使用者指定的「昭和のグラゼニ-raw-【第92話】」資料夾；21 頁原圖均為 1121 × 1600。原圖唯讀，瀏覽器透過測試頁的本機檔案選擇器載入 Blob，保存副本僅存在測試頁記憶體，不寫回來源或正式項目。圖片不納入 Git。合成圖的隔離測試不能取代以下真圖測試。

### 真實像素演算法與一致性

子代理 B 對第 3、7、16 頁分別測試原圖座標 `(280,400)`、`(560,800)`、`(840,1200)`，容差 28，擴展 0／16 px，待修補添加，共 18 組：輸出三個圖層全部與原核心逐位元組一致；18 次懸停預覽均不修改正式狀態。選取面積從 6 到 336,811 像素，涵蓋小連通區及大範圍。

| 設定 | 正式計算 | 魔法棒預覽＋左右合成 |
| --- | --- | --- |
| 擴展 0 | 47.43–73.88 ms | 105.20–131.87 ms |
| 擴展 16 | 319.87–327.94 ms | 358.52–391.19 ms |

另外 4 組純色填充也與原核心一致。第 7 頁 `(280,400)` 為實際白底 RGB(255,255,255)，選取 282,748／336,811 像素：擴展 0／16 時正式計算為 140.55／402.34 ms，預覽合成為 208.97／465.54 ms，包含周邊顏色取樣。第 16 頁額外座標 `(980,880)` 實際為深色，記錄為深色連通區樣本，不宣稱白色氣泡樣本。

上述是直接在 Node 執行 Worker 純核心的計時，不包含瀏覽器傳輸及 OffscreenCanvas 轉換；用途是確認真圖運算負載和結果一致性，不當作瀏覽器輸入延遲。

### 真圖瀏覽器互動

主代理在 IAB 載入第 16 頁（密集網點、白底文字區和封閉氣泡），Worker 回覆仍刻意延後 1000 ms，暖機後量測：

- 魔法棒容差 28、擴展 16：連續點選氣泡／白底／背景後立即切換矩形拖曳。14 個 pointer-to-SVG 樣本為 24.6–41.4 ms，無 >50 ms 主執行緒長任務。
- 純色填充、容差 45、擴展 16：兩次魔法棒選取後立即切換畫筆。13 個樣本為 26.7–40.5 ms，無長任務，最大 rAF 間隔約 42.7 ms。
- 真圖局部視窗：建立副本、矩形減選、套用，確認視窗關閉且主頁自動保存成功。
- 已完成實際編輯副本保存並重開；圖上魔法棒、矩形和填色筆畫都保留，狀態為已保存，無錯誤。先前另已走過兩筆畫筆→撤銷第二筆→保存重開流程。

觀察到背景計算尚未結束時可保留多筆待處理輪廓並繼續操作。這是小樣本互動驗證；仍不宣稱所有硬體與瀏覽器均達 60 FPS。

## 發布限制

尚未部署、推送或建立 Tag。尚不能將本機合成圖的自動化測試等同所有實際漫畫、硬體、瀏覽器和 AutoDL 代理的性能驗收。正式發布前需在實際 URL 測試同源 module Worker 載入、OffscreenCanvas 與 PNG 保存；本版不需要 SharedArrayBuffer 或 COOP／COEP。


## 魔法棒與畫筆追加修正（2026-09-21）

使用者回報點擊等待太久、懸停預覽消失。舊測試頁 `workerDelay=1000` 在每次回覆加入一秒延遲，且舊 UI 每次 hover 都使正式 render 世代失效。現在正式 left/right 與 magicLeft 分離，hover 只作廢 magicLeft；提交立即排程 render，不再等待 metadata 回傳才發起下一步。魔法棒预览預設開啟，可關閉並記住偏好；離開或關閉立即隱藏疊層。測試頁默认延遲為 0，可即時切換慢速模擬，關閉也釋放排隊回覆。

Worker 單項快取包含 revision、seed、容差、擴展、操作、類別、ROI 與 intersectOffset。對相同預覽直接提交可採用已算好的 next layers；init、commit、undo、redo、merge 使快取失效。新增測試驗證全部 key、失效路徑、乾淨 frame 與原核心逐像素一致，回收測試包括第三張 magic bitmap。

Sol high 子代理實測同一真圖：第 7 頁白區 solid expand=0 正式冷計算 140.70 ms → 預覽命中提交 2.25 ms；expand=16 為 399.03 → 2.47 ms。第 16 頁測點為 78.05 → 3.72 ms、337.52 → 2.38 ms。4/4 結果逐像素一致。這些是 Node 核心耗時，不包括瀏覽器合成／傳輸，也不代表首次預覽已只需數毫秒。

主代理在 IAB、真實 16.jpg、人工延遲 0 ms、容差 28／擴展 16 實際看到白底文字區綠色懸停，點擊後變為正式粉色待修補標記，關閉預覽立即隱藏。首次瀏覽器量測中一次預覽後點擊到正式 canvas 更新約 262 ms；這不是核心快取耗時，也不能據此聲稱所有點擊都在此時間完成。新的 `pointerToCanvasFrame` 與 pointer-to-SVG 分列，前者是 pointerup 到正式 canvas 提交的時間，並非螢幕實際出光時間；拖曳案例不能拿它當 pointerdown 到完成的延遲。

畫筆提供 1–200 px 滑塊／數字雙向同步、每次 4 px 的 `[` 縮小與 `]` 放大，工具列顯示文字提示。快捷鍵不攔截文字輸入或 Ctrl／Cmd／Alt 組合。IAB 實測 16→20→16，滑塊按右方向鍵一步變 17，數字改 40 後滑塊亦為 40。

本次再驗證：前端 lint／build 與 88 項測試通過；後端 236 項在沙箱通過，另 2 項程序恢復測試因 ps 權限失敗後以允許程序讀取的環境重跑通過。獨立 Sol high 複審未發現阻塞問題。真圖畫筆修改保存並重開成功。

追加快捷鍵：魔法棒使用 `[` 減少、`]` 增加容差（每次 1，範圍 0–100），畫筆仍調整筆刷大小。工具列附對應文字，輸入框保持原本輸入行為。


## 第二輪：依序實施排程、局部重繪與選區核心優化

起點為 `07b2c0b`，仍在 `codex/selection-responsive` 工作樹。三個 Sol high 子代理分別負責局部 render、選區核心／獨立 frozen oracle、管線正確性審核；主代理負責 scheduler、協定／UI 整合與瀏覽器驗證。

- 排程：正式命令與 snapshot 維持 FIFO，document render 次之，preview 最後；同類 render 只保留最新。取消有 request id 邊界，預覽失效不拋錯；UI 各自管理 document/preview 在途請求。已開始的同步 CPU 工作仍需算完，尚未加入協作式中斷。
- 局部重繪：每次圖層變更以實際 RGBA diff 求 bbox；以最多 64 個 revision 追蹤變化。patch 必須與 UI 實際已呈現基底匹配；漏幀會合併變化，未知基底／顯示設定變化則完整更新。幾何外可能受影響的同色採樣和局部交集也納入變更。無變化用 1×1 同值 patch。
- 選區核心：固定 seed 洪水填充內聯鄰域迴圈；擴展／收縮使用連續列區段與差分區間，維持既有連通性、round／floor 和邊界行為。700 個隨機 palette 圖、700 個隨機 Mask 與固定舊核心比對；真圖第 3／7／16 頁容差 28、擴展 0／16／32 共 9 組逐像素相同。

真圖核心三次 serial median（不含資料傳输、合成、UI）：

| 頁／測點 | 擴展 | 舊核心 ms | 新核心 ms |
| --- | ---: | ---: | ---: |
| 3／840,1200 | 0 / 16 / 32 | 7.94 / 219.12 / 409.84 | 2.81 / 26.03 / 28.51 |
| 7／280,400 | 0 / 16 / 32 | 8.84 / 206.71 / 383.65 | 5.95 / 11.68 / 12.89 |
| 16／560,800 | 0 / 16 / 32 | 0.94 / 222.73 / 428.07 | 0.71 / 10.77 / 9.44 |

1121×1600、32×32 dirty 區域的 Node 渲染樣本：4 次全圖合成共 161.97 ms、局部 1.84 ms；雙 view 傳輸像素量縮小約 1752 倍。取得實際 dirty bbox 仍需掃描圖層，4 次最壞無變化掃描共 39.06 ms（約 9.8 ms／次），不能將局部 render 的加速直接當作整個操作加速。

本輪仍有完整選區陣列、完整撤銷快照與完整 magic 預覽合成；工作量變小和優先級調整不等於所有圖像都已保證 60 FPS。


瀏覽器整合驗證（真圖 16.jpg，1121×1600，人工延遲 0 ms，容差 28／擴展 16）：修正量測起點為 magic pointerdown、其他手勢 pointerup，正式畫面版本必須大於操作開始基底。三個點擊的 pointerdown→正式 canvas 更新為 121.0、106.6、203.1 ms；該段 pointer→SVG 回饋 36.0–39.8 ms，無 >50 ms 主執行緒長任務。這是 IAB 約 24 Hz 環境的有限樣本，不能等同所有漫畫或 60 FPS 驗收。另已看到獨立綠色懸停預覽；兩筆 40 px 筆畫→撤銷第二筆→保存重開後，三個魔法棒標記及第一筆保留，第二筆未出現，左右畫面一致。

獨立複審確認排程、snapshot barrier、取消邊界、patch baseline fallback 和 bitmap 釋放路徑無阻塞問題。後端完整 238 tests 通過（具備程序檢視權限），前端整合首輪 104 tests／lint／build 通過。沒有變更 GPU 工作流、原始漫畫或其他分支。


密集網點補測發現純區段算法在 1121×1600 棋盤格擴展 16 出現回退（約 535 ms，舊算法 144 ms）。已加入依來源 run 密度選擇算法：runs 超過 pixels/3 時使用等價 prefix 路徑。補測棋盤格、70% 閾值噪音、全白 × 擴展 16／32，冷／熱交錯各三次，全部與 frozen oracle 一致。修正後棋盤格 expand16 舊／新熱 median 119.59／111.07 ms，expand32 194.77／183.54 ms；噪音為 158.04／94.87 ms、232.62／133.87 ms；全白 141.38／44.26 ms、216.80／43.69 ms。這些實測說明收益取決於選區形態，不能對所有漫畫宣稱同倍數加速。


最終檢查：105/105 前端測試、lint、正式 build、git diff --check 通過；後端 238/238 通過。最後整套真圖核心樣本：expand16／32 舊約 208–436 ms，新約 12–18 ms（前述三次 median 與本次單套結果受暖機與負載影響，不混算倍率）。已重新載入最新 Worker，IAB 真圖另一次首次點擊→正式畫面 116.2 ms、無主執行緒長任務。試用頁保留在 `?workerDelay=0`，刷新也不會重新啟用人工一秒延遲。第二輪改動尚未提交、推送或部署。


## 工具游標與相鄰原圖預載

矩形、畫筆、魔法棒各有不同原生 SVG 游標，十字中心為落點；畫筆仍顯示實際尺寸圓圈。游標移動由瀏覽器直接處理，不等待像素運算。

第一部分根據篩選後頁序預載當頁、後兩頁、前一頁。背景只 fetch／解碼 ImageBitmap，使用時才提取 ImageData；已提取原圖可直接重用。LRU 解碼快取預算 64 MiB（不代表整個編輯器記憶體上限）；超大圖不預載，正式讀取仍可進行但不保留於快取。離開項目會清理，inflight 舊結果不能污染新快取。原圖尺寸不符會報錯，禁止靜默縮放。可編輯圖層仍按 revision 載入。

使用 `tests/source-preload-browser.html` 在 IAB 選入使用者第 15–18 頁真圖（各 1121×1600），不預先讀圖探測尺寸。15→16→17→16→15：第一次原圖讀取 20.7 ms；預載命中第 16／17 頁為 4.4／4.9 ms；返回已提取頁均為 0.1 ms。四張只解碼四次，快取 28,697,600 bytes（約 27.4 MiB），重訪像素抽樣簽章一致、無載入錯誤。這是本機 Blob URL 的原圖讀取樣本，不包含網路遠端延遲、保存、圖層載入、Worker 初始化或整頁切換耗時，不承諾整頁數毫秒。實際 RasterEditor 另使用第 16 頁確認三個工具的原生 cursor 均不同。

本次最終驗證：112/112 前端測試、238/238 後端測試、lint、正式 build、git diff --check 通過。獨立 Sol high 複審無阻塞問題。改動留在同一獨立工作樹，尚未提交或部署。


## 完整切頁量測、Worker 重用與初始化複製

本輪從 `9848457` 開始。主代理負責完整切頁計時與 Worker owner；Sol high 子代理分別處理 init ownership、真正 ProjectWorkbench 測試 fixture、獨立生命週期與排序審核。

新 `tests/page-navigation-browser.html` 掛載真正 ProjectWorkbench／RasterEditor，使用使用者第 15–18 頁（1121×1600）；本機 fixture 模擬項目 API、空白初始 revision 圖層和保存，原圖是實際 JPEG。啟用 React StrictMode；baseline 與 optimized 共用相同 API、原圖預載與顯示方式，僅前者保留每頁新 Worker／雙重複製。測試頁本身的 React、API 記錄與 StrictMode 重複 effect 都有額外成本，因此不是正式環境絕對延遲或遠端網路驗收。

先取得各段基準，再實作：項目主編輯器延遲取得共用 Worker，切頁以 FIFO init 重置文件，舊 render／preview token 失效；不再建立新執行緒。轉移 overlay／other／edited／detectedText 後，主執行緒只保留其 wrapper 作初始化狀態判斷，不讀已 detach 資料；Worker 直接接管收到的隔離輸入，省第二次 slice。Base 保留在原圖快取，送 Worker 仍複製一次。純 engine API 預設 copy，保留呼叫者資料隔離。

相同頁序 15→16→17→18→17→16→15，共六次切頁：

| 項目 | Baseline | Optimized |
| --- | ---: | ---: |
| 完整切頁 ms（逐次） | 304.9, 305.5, 305.8, 205.1, 179.1, 206.8 | 266.7, 182.3, 244.7, 183.0, 155.4, 215.1 |
| 完整切頁中位數 | 255.85 ms | 199.05 ms |
| Worker init 中位數 | 62.35 ms | 9.55 ms |
| Worker 建立／終止／存活 | 7 / 6 / 1 | 1 / 0 / 1 |
| 每頁 init 像素複製量（無 detectedText，程式路徑推算） | 57,395,200 bytes | 7,174,400 bytes |

此樣本完整切頁中位數改善約 22%，Worker init 約 85%；仍有單次回退，不能對所有圖片宣稱固定倍率。初次開頁仍要載圖及啟動 Worker，這次約 336／341 ms，沒有顯著改善。assets.total 與 firstFrame 已成為主要剩餘成本，並行 asset durations 不應相加。首次非 StrictMode 探索數字不與此表混算。

畫筆 80 px 點下後立刻切頁：save.snapshot 41.1 ms、save.upload 29.9 ms、save.wait 71.1 ms，之後才 project.reload／下一頁 init；返回第 15 頁顯示 saves=1、revision=2，左右畫布可見粉色筆畫。這裡 upload 是 fixture API 保存，不能代表遠端上傳。離開編輯到批量階段後 Worker 存活數從 1 降到 0，返回後才建立第 2 個 Worker，內容仍保留。

新增測試涵蓋 owned buffer transfer 不 detach 原圖、預設 copy／adopt 對等、跨頁舊幀回收、lazy owner／terminal replacement、snapshot→init FIFO 屏障與文件歷史重置；計時記錄涵蓋失敗與晚回 callback，不讓診斷 listener 改變任務結果。

本輪最終驗證：122/122 前端測試、238/238 後端測試、lint、正式 build、git diff --check 通過。獨立複審無阻塞問題。性能改動未提交、推送或部署。
